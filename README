Whats this?

This Files are Part of the software collection to build own Images
of OpenMCT Firmware.

First you have to run ./prepare.sh .
After this run the builded configure with the arguments you want.
Finally just type 'make' an wait some fee minutes.

After all you will find in ~/openmct-build/images the separates Images.

carsten
