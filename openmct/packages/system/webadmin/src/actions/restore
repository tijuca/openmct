. ../actions/constants
. ../actions/functions
html
createdir $ARCHIVEDIR
thead
if [ "$FORM_archive" != "" ]; then
 vexec "tar xvf $FORM_archive -C /"
else
 echo "ERROR: archive not selected!"
fi
tfoot
