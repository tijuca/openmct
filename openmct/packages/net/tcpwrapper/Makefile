# TCP Wrapper
DOWNLOAD=	ftp://ftp.porcupine.org/pub/security/tcp_wrappers_7.6.tar.gz

MAKEARGS=	CC=$(OUTPUTDIR)/$(TARGET)/bin/$(TARGET)-uclibc-gcc REAL_DAEMON_DIR=/tmp linux

INSTALL=	true

include		$(OPENMCT_DEVEL_PREFIX)Rules
